# LLM Provider API Keys (at least one required for AI features)
# The system will automatically detect and use the first available provider

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Groq Configuration (Alternative to OpenAI)
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini Configuration (Alternative to OpenAI/Groq)
GOOGLE_API_KEY=your_google_api_key_here
# OR
GEMINI_API_KEY=your_gemini_api_key_here

# Note: You only need ONE of the above API keys for the system to work
# The system will automatically prioritize providers in this order:
# 1. OpenAI (if OPENAI_API_KEY is set)
# 2. Groq (if GROQ_API_KEY is set)  
# 3. Gemini (if GOOGLE_API_KEY or GEMINI_API_KEY is set)
